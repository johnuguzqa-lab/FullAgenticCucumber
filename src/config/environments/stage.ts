import { EnvironmentConfig } from '../../types';

/** Stage environment configuration. Replace with the real ZincBank staging URL. */
export const stageConfig: EnvironmentConfig = {
  name: 'stage',
  baseUrl: 'https://stage.zincbank.cydeo.io',
};
